/** this is a comment line in the sample program **/
INT f2(INT x, INT y ) 
 BEGIN 
    INT z;
    INT i;
    z := x*x - y*y;
    FOR (i:=1; i<=z; i:=i+1;);
    RETURN z; 
 END

INT MAIN main()
BEGIN
	REAL x;
	x:=1.2;
	INT y;
	IF (x==1)
		IF (x==f2(2,y))
			WRITE(3,"stdout");
		ELSE READ(x,"stdout");
	/**RETURN x;**/
END